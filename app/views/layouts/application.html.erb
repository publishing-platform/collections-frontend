<%= render "publishing_platform_publishing_components/components/layout", {
  title: "#{yield(:title).presence || @content_item.title} - Publishing Platform"
} do %>

  <% if yield(:breadcrumbs).present? %>
    <%= yield(:breadcrumbs) %>
  <% end %>
  
  <%= yield %>
<% end %>
